const usersData = require('./users');
const booking = require("./booking")
const landing = require("./landing");

module.exports = {
    usersdata: usersData,
    booking: booking,
    landing : landing
};
